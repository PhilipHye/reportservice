# Copy to .env in each package as needed.
# Root-level values shared across the demo:

# Data Connector
CONNECTOR_PORT=4001

# Report Service
REPORT_SERVICE_PORT=4000
CONNECTOR_URL=http://localhost:4001
INTERNAL_API_KEY=dev-local-shared-secret
